# AssemblyAI — speech-to-text (server-side only, never exposed to the browser)
ASSEMBLYAI_API_KEY=

# Anthropic — structured extraction + evidence-grounded answers (server-side only)
ANTHROPIC_API_KEY=
# Optional override. Defaults to claude-opus-5.
# ANTHROPIC_MODEL=claude-opus-5
