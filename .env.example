# AssemblyAI — speech-to-text (server-side only, never exposed to the browser)
ASSEMBLYAI_API_KEY=

# OpenAI — structured extraction + evidence-grounded answers (server-side only)
OPENAI_API_KEY=
# Optional override. Defaults to gpt-5.5.
# OPENAI_MODEL=gpt-5.5
